# build artifacts
*.o
*.obj
*.out
a.out
# compiled engine binaries (build from source)
src/carmichael
src/carmichael_m1
src/carmichael_m2
src/carmichael_star
src/carmichael_star_rec
src/carmichael_par
src/mitm
src/mitm64
src/mitm64_fixed
# latex build artifacts (keep .tex and .pdf)
paper/*.aux
paper/*.log
paper/*.toc
paper/*.synctex.gz
# python
__pycache__/
*.pyc
.venv/
# run state (large / machine-specific)
run_k*.log
checkpoint_k*.json
*.tmp
.DS_Store

# frozen artifact archive (2026-09-11): tarballs + pulled run logs, not for git
archive/
